## Summary

What changed and why? One or two sentences.

## Test plan

- [ ] Public CI gates pass locally (`uv sync --locked --all-groups`, then the steps in
      `.github/workflows/ci.yml`) or the GitHub Actions run is green
- [ ] Added or updated tests (if behavior changed)
- [ ] Docs updated (if user-facing)

## Notes

Optional: issue links (#123).
